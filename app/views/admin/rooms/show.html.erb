<h2>Details</h2>
<div class="piece">
  <% if @room.cover.attached? %>
    <%= image_tag @room.cover.variant(resize: "300x300") %>
  <% end %>
  <div class="piece-right">
    <h2><%= @room.name %></h2>
    <p><b>Amount of beds:</b> <%= @room.amount_of_beds %></p>
    <p><b>Price:</b> US $ <%= @room.price %></p>
    <p><b>Hotel:</b> <%= @room.hotel.name %></p>
    <p><b>Contact</b>: <%= mail_to @room.hotel.email, "Contact" %></p>
  </div>
</div>

<div class="controls">
  <%= link_to "« back", admin_rooms_path, :class => "btn btn-back" %>
  <%= link_to "✎ edit", edit_admin_room_path(@room), :class => "btn btn-edit" %>
  <%= link_to "🗑 delete", admin_room_path(@room), method: :delete, data: { confirm: "Do you really want to delete this room?" }, :class => "btn btn-delete" %>
</div>
