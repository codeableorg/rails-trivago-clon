<div class="piece">
  <div>
    <h2><%= @hotel.name %></h2>
    <% if @hotel.cover.attached? %>
      <%= image_tag @hotel.cover.variant(resize: "200x200") %>
    <% end %>
  </div>
  <div class="piece-right">
    <p><b>Contact:</b> <%= mail_to @hotel.email, "Contact" %></p>
    <p><b>City:</b> <%= @hotel.city %></p>
    <p><b>Country:</b> <%= @hotel.country %></p>
    <p><b>Address:</b> <%= @hotel.address %></p>
  </div>
</div>
<div class="controls">
  <%= link_to "« back", admin_hotels_path, :class => "btn btn-back" %>
  <%= link_to "✎ edit", edit_admin_hotel_path(@hotel), :class => "btn btn-edit" %>
  <%= link_to "🗑 delete", admin_hotel_path(@hotel), method: :delete, data: { confirm: "Do you really want to delete this hotel?" }, :class => "btn btn-delete" %>
</div>
