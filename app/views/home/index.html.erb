<p>Welcome</p>

<% if current_user && current_user.role == "admin" %>
    
<% else  %>
<table>
  <thead>
    <tr>
      <th>From</th>
      <th>To</th>
      <th>Room</th>
      <th>Paid Price</th>
      <th>User</th>
    </tr>
  </thead>
  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.start_date %></td>
        <td><%= booking.end_date %></td>
        <td><%= booking.room_id %></td>
        <td><%= booking.paid_price %>$</td>
        <td><%= booking.user.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% end  %>